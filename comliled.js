"use strict";

var number = [1, 2, 3];
var doubleNumbers = numbers.map(function (number) {
  return number * 2;
});
console.log(doubleNumbers);

var itme = 0;
input.map(function (item) {
  return item + 1;
});
